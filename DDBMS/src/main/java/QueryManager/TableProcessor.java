package QueryManager;

public class TableProcessor {
    private String path = null;
    public TableProcessor(String path) {
        this.path = path;
    }

    public void performOperation(String query) {
        // take this method as starting point and start processing the query, after the DistributedDatabaseLayer is completed, we will have the api to be hitted
    }
}
